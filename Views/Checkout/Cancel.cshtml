@{
    ViewData["Title"] = "Payment cancelled";
}

<div class="text-center mt-5">
    <h1>Payment cancelled</h1>
    <p>Your basket is still available if you want to try again.</p>
    <a asp-controller="Basket" asp-action="Index" class="btn btn-primary">Return to basket</a>
</div>
